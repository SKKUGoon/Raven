[server]
host = "0.0.0.0"
port_binance_spot = 50001
port_binance_futures = 50002
port_binance_futures_klines = 50003
port_timebar_minutes = 50051
port_timebar_seconds = 50052
port_tibs_large = 50053
port_tibs_small = 50054
port_trbs_small = 50055
port_trbs_large = 50056
port_vibs_small = 50057
port_vibs_large = 50058
port_vpin = 50059
port_tick_persistence = 50091
port_bar_persistence = 50092
port_kline_persistence = 50093


[influx]
url = "http://localhost:8086"
token = "admin-token-banana"
org = "influx"
bucket = "influx"
batch_size = 1000
batch_interval_ms = 1000

[timescale]
url = "postgres://postgres:postgres@localhost:5432/postgres"
schema = "warehouse"

[logging]
level = "info"

[routing]
# Optional: venue selection defaults to all known venues if this list is empty.
venue_include = []
venue_exclude = []

# Optional: venue-specific symbol overrides.
# Example: spot uses PEPEUSDT but futures uses 1000PEPEUSDT.
[routing.symbol_map]
"PEPE/USDT" = { BINANCE_FUTURES = "1000PEPEUSDT" }

[binance_klines]
interval = "5m"
shard_size = 50
connections = 10
